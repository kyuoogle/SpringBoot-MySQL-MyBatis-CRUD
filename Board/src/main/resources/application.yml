# ?????? ?? -> ???? 8080
server:
  port: 8080

#database ?? ??
spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/yk_board?serverTimezone=Asia/Seoul&characterEncoding=UTF-8
    username: y2k
    password: 990719

# MyBatis
mybatis:
  mapper-locations: classpath:/mappers/*.xml
  # DTO ??? (BoardDTO ?? ???)
  type-aliases-package: com.ykk.board.dto